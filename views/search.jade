extends layout
block content
    style.
        .item {
            border: 1px solid var(--color-theme);
            padding: 0.5rem;
            cursor: pointer;
        }
        .item:hover {
            background-color: var(--color-section)
        }
        .item * {
            margin: 0;
            flex: 100%
        }
        .item .row {
            display: flex;
        }
        .item h2{
            flex: 80%;
            color: var(--color-theme)
        }
        .item code {
            flex: 20%
            color: var(--color-highlight);
        }
        .author {
            color: var(--color-highlight)
        }
    form(action="/search", method="post")
        fieldset
            legend Search for an item in FSS
            input(name="query", type="text", value=query, style="width: 100%")
            br
            //- input(name="type", type="radio", value="storage", checked)
            //- lable Minecraft items and blocks
            //- input(name="type", type="radio", value="coord")
            //- lable Coordinate
            input(name="type", type="radio", value="library", checked)
            lable Books
            button(type="submit" style="float: right") Search
    p Please noted that you have to search for the exact phrase. I am really really sorry about that.
    if (ans)
        each item in ans
            div(class="item", onclick="location.href='/item/"+database+"?id="+item.id+"'")
                div(class="row")
                    h2= item.name
                    code= item.id
                p(class="author")= item.author
                p= item.desc